---
import Layout from '../layouts/Layout.astro';
import MainGridLayout from '../layouts/MainGridLayout.astro';
import { siteConfig } from '../config';
---

<Layout title="移动端Banner测试">
	<MainGridLayout>
		<div class="mobile-test-info">
			<h1>移动端Banner优化测试</h1>
			<p>请在移动设备或浏览器开发者工具的移动模式下查看此页面，测试以下功能：</p>
			<ul>
				<li>✅ 响应式banner高度调整</li>
				<li>✅ 移动端文字叠加层优化</li>
				<li>✅ 触摸手势轮播支持</li>
				<li>✅ 性能优化（硬件加速）</li>
				<li>✅ 横屏模式适配</li>
				<li>✅ 暗色模式优化</li>
			</ul>
			
			<div class="test-instructions">
				<h2>测试说明：</h2>
				<ol>
					<li><strong>响应式测试：</strong>调整浏览器窗口大小，观察banner高度和文字大小的变化</li>
					<li><strong>触摸测试：</strong>在移动设备上左右滑动banner进行轮播切换</li>
					<li><strong>性能测试：</strong>观察滚动和动画的流畅度</li>
					<li><strong>横屏测试：</strong>旋转设备到横屏模式，检查布局适配</li>
				</ol>
			</div>

			<div class="optimization-details">
				<h2>优化详情：</h2>
				<div class="optimization-grid">
					<div class="optimization-item">
						<h3>🎨 视觉优化</h3>
						<ul>
							<li>不同屏幕尺寸的banner高度适配</li>
							<li>文字大小和间距的响应式调整</li>
							<li>增强的文字阴影效果</li>
							<li>暗色模式下的渐变背景优化</li>
						</ul>
					</div>
					
					<div class="optimization-item">
						<h3>⚡ 性能优化</h3>
						<ul>
							<li>硬件加速（GPU渲染）</li>
							<li>减少重绘和重排</li>
							<li>优化的图片渲染</li>
							<li>触摸事件优化</li>
						</ul>
					</div>
					
					<div class="optimization-item">
						<h3>📱 交互优化</h3>
						<ul>
							<li>触摸手势轮播支持</li>
							<li>防止意外的图片拖拽</li>
							<li>优化的触摸响应区域</li>
							<li>轮播指示器（可选）</li>
						</ul>
					</div>
					
					<div class="optimization-item">
						<h3>🔧 技术优化</h3>
						<ul>
							<li>CSS contain属性优化</li>
							<li>will-change属性预优化</li>
							<li>backface-visibility优化</li>
							<li>媒体查询精确控制</li>
						</ul>
					</div>
				</div>
			</div>
		</div>
	</MainGridLayout>
</Layout>

<style>
	.mobile-test-info {
		max-width: 800px;
		margin: 2rem auto;
		padding: 2rem;
		background: var(--card-bg);
		border-radius: 12px;
		box-shadow: var(--shadow);
	}

	.mobile-test-info h1 {
		color: var(--primary);
		margin-bottom: 1rem;
		font-size: 2rem;
	}

	.mobile-test-info h2 {
		color: var(--text-color);
		margin: 2rem 0 1rem 0;
		font-size: 1.5rem;
	}

	.mobile-test-info h3 {
		color: var(--text-color);
		margin: 1rem 0 0.5rem 0;
		font-size: 1.2rem;
	}

	.mobile-test-info p, .mobile-test-info li {
		color: var(--text-color-secondary);
		line-height: 1.6;
		margin-bottom: 0.5rem;
	}

	.mobile-test-info ul, .mobile-test-info ol {
		padding-left: 1.5rem;
	}

	.test-instructions {
		background: rgba(var(--primary-rgb), 0.1);
		padding: 1.5rem;
		border-radius: 8px;
		margin: 2rem 0;
		border-left: 4px solid var(--primary);
	}

	.optimization-details {
		margin-top: 2rem;
	}

	.optimization-grid {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
		gap: 1.5rem;
		margin-top: 1rem;
	}

	.optimization-item {
		background: rgba(var(--text-color-rgb), 0.05);
		padding: 1.5rem;
		border-radius: 8px;
		border: 1px solid rgba(var(--text-color-rgb), 0.1);
	}

	.optimization-item h3 {
		margin-top: 0;
		color: var(--primary);
	}

	.optimization-item ul {
		margin: 0;
		padding-left: 1.2rem;
	}

	.optimization-item li {
		margin-bottom: 0.3rem;
		font-size: 0.9rem;
	}

	/* 移动端样式 */
	@media (max-width: 768px) {
		.mobile-test-info {
			margin: 1rem;
			padding: 1.5rem;
		}

		.mobile-test-info h1 {
			font-size: 1.5rem;
		}

		.optimization-grid {
			grid-template-columns: 1fr;
			gap: 1rem;
		}

		.optimization-item {
			padding: 1rem;
		}
	}
</style>